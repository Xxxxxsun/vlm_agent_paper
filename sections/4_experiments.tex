% ============================================================
%  §4  Experiments
% ============================================================
\label{sec:experiments}

% ============================================================
\subsection{Experimental Setup}
\label{sec:setup}
% ============================================================

\paragraph{Benchmarks.}
We evaluate on four standard visual reasoning benchmarks that stress different parts of visual
reasoning.
\textbf{ChartQA}~\citep{masry2022chartqa} requires multi-step numerical reasoning over
chart images (test split: 2{,}500 questions; evaluation metric: relaxed accuracy with a
5\% numeric tolerance).
\textbf{MathVista}~\citep{lu2023mathvista} tests mathematical reasoning grounded in
figures, diagrams, and scientific plots (testmini split: 1{,}000 questions; accuracy).
\textbf{HRBench}~\citep{chen2024hrbench} probes perception on high-resolution
(${\geq}4\mathrm{K}$) images requiring sub-region localisation and fine-grained visual
disambiguation (accuracy).
\textbf{V$^*$}~\citep{wu2024vstar} measures visual search ability: given a query about
an object's property, the agent must locate and attend to the relevant sub-image before
reasoning (${\approx}500$ questions; accuracy).
These benchmarks were chosen because they create different opportunities for capability
generation: ChartQA and MathVista often require structured extraction and multi-step
reasoning, while HRBench and V$^*$ often benefit from sub-region inspection or higher-resolution
visual access.

\paragraph{Foundation models.}
Experiment~I evaluates six proprietary and open-weight VLMs:
\textbf{GPT-4o}, \textbf{o4-mini}, \textbf{GPT-5.4}, \textbf{Doubao-Seed-2.0},
\textbf{Qwen3.5-27B}, and \textbf{Gemini-3.1-Pro-Preview}.
Experiment~II evaluates \textbf{GPT-4o}, \textbf{GPT-5.4}, and \textbf{Gemini3Flash}
on the GTA benchmark.
We additionally report a controlled Doubao-Seed-2.0-Pro GTA analysis to inspect how
provided-tool skills change with tool abstraction and task environment.
Experiment~III uses the \textbf{Qwen2.5-VL} family (3B, 7B, 32B) to align with the
VTool-R1 evaluation protocol.
All VLM weights are \emph{frozen} throughout; no gradient updates are performed.

\paragraph{Evolution protocol.}
For each benchmark, we sample a training subset of $k{=}200$ cases from the official
training split and run the \sysname{} evolution loop for $N{=}3$ iterations.
At the end of training, the capability set $\mathcal{C}$ is frozen and evaluated on
the held-out validation / test split.
We report the average over three independent random seeds (different $k{=}200$ subsets)
to account for sampling variance where repeated runs are available.

\paragraph{Baselines.}
For Experiment~I, we compare four evolution configurations that isolate the contribution
of each component:
\textbf{None (Base Agent)} invokes the frozen VLM with no evolved capabilities — the
zero-shot baseline;
\textbf{Skill Only} runs the evolution loop but restricts the Generator to producing
reasoning skills (no tool generation);
\textbf{Tool Only} restricts generation to executable visual tools (no skill generation);
\textbf{Skill $+$ Tool} is the full \sysname{} system (co-evolution of both capability
types).

\paragraph{Evaluation metrics.}
All primary metrics are task-level accuracy on the held-out split.
Experiment~II additionally reports four step-level metrics on the GTA benchmark:
\textbf{ToolAcc} (fraction of steps with correct tool selection),
\textbf{ArgAcc} (correct argument specification given the correct tool),
\textbf{InstAcc} (overall instruction-following fidelity), and
\textbf{AnsAcc} (final answer accuracy).
For the supplementary controlled GTA analysis, we also report tool usage rate, average number
of tool calls, and dominant tool-call chains.
Experiment~III follows the VTool-R1 evaluation protocol~\citep{vtoolr1_2025} on Chart
and Table reasoning splits.

% ============================================================
\subsection{Experiment~I: Autonomous Evolution from Scratch}
\label{sec:exp1}
% ============================================================

\paragraph{Motivation.}
The central claim of \sysname{} is that a VLM agent can bootstrap useful capabilities
from failure on a small training subset, without any weight updates.
Experiment~I tests this claim in the most challenging setting: the capability library
is initialised to empty ($\mathcal{S}{=}\emptyset$, $\mathcal{T}{=}\emptyset$), and the
system must generate, validate, and accumulate all capabilities autonomously.
By evaluating six foundation models, with some ablation cells still pending, we assess
whether the observed gains are tied to a single backbone or appear across model families.


\paragraph{Qualitative inspection of evolved capabilities.}
In addition to aggregate accuracy, we inspect three representative forms of evolved capability:
(i) a ReFocus-style chart/table editing case, where a generated tool marks or masks visual
evidence before reasoning; (ii) a ZoomEye-style high-resolution search case, where a generated
tool performs coarse-to-fine region inspection; and (iii) an interleaved visual skill, where the
retrieved skill contains both a textual SOP and visual evidence from a prior failure. These case
studies are qualitative: they support the claim that \sysname{} can generate capability
structures resembling useful prior motifs, not that it reproduces the full algorithms of
ReFocus~\citep{refocus_2025} or ZoomEye~\citep{zoomeye_2024}. Appendix~\ref{app:capability_cases}
gives the detailed write-up and marks the exact fields to fill with the corresponding generated
artifacts.

\paragraph{Multi-model comparison.}
Table~\ref{tab:evolution_full} reports the completed model--strategy entries. Blank cells
indicate runs whose results have not yet been inserted and are excluded from aggregate
statements. Comparing the full Skill~$+$~Tool row with the base agent, \sysname{} improves
direct inference in 21 of 23 completed model-benchmark cells, with an average gain of 4.8
accuracy points. The largest gains occur on Gemini-3.1-Pro-Preview / ChartQA (+14.6),
o4-mini / HRBench (+11.6), GPT-5.4 / HRBench (+10.3), GPT-5.4 / V$^*$ (+9.9), and
Doubao-Seed-2.0 / V$^*$ (+8.6).

The table also shows why the framework should not be interpreted as a monotonic ``add more
capabilities'' recipe. Full co-evolution is not the best entry in every cell: for GPT-4o on
MathVista and ChartQA, Skill Only is slightly higher than Skill~$+$~Tool; for o4-mini on
ChartQA and Qwen3.5-27B on V$^*$, the base agent remains higher than the full system. Tool Only
can also be harmful, most visibly for o4-mini on HRBench. These exceptions are important: they
motivate validation and mastery rather than unconditional tool use, and they suggest that the
benefit of a generated capability depends on both the benchmark and the backbone's ability to
follow the resulting SOP or tool policy.

A cautious pattern is nevertheless visible. Skill-only evolution is often competitive on
ChartQA and MathVista, where improvements can plausibly come from better decomposition and
calculation procedures. Tool-related variants tend to help more on HRBench and V$^*$, where
sub-region inspection and visual search are more central. We treat this as evidence that the
cognitive/perceptual distinction is a useful design rule for capability generation, not as a
standalone proof that every error in these benchmarks has that structure.

\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{llcccc}
\toprule
\textbf{Model} & \textbf{Evolution Strategy}
  & \textbf{HRBench}
  & \textbf{MathVista}
  & \textbf{V$^*$}
  & \textbf{ChartQA} \\
\midrule

\multirow{4}{*}{GPT-4o}
  & None (Base Agent)  &0.6386  &0.6711  &0.5828  &0.7552  \\
  & Skill Only         &0.66142  &0.7077  &0.63576  &0.7796  \\
  & Tool Only          &0.6414  &0.6922  &0.6159  &0.7693  \\
  & Skill + Tool       &0.66  &0.70777  &0.6423  &0.775  \\

\midrule

\multirow{4}{*}{o4-mini?}
  & None (Base Agent)  &0.73  &0.753333  &0.72847  &0.7833  \\
  & Skill Only         &0.7671  &0.75666  &0.75496  &0.76927  \\
  & Tool Only          &0.5529  &0.6978  &0.7677  &0.7677  \\
  & Skill + Tool       &0.8457  &0.7622  &0.7947  &0.7917  \\

\midrule

\multirow{4}{*}{gpt5.4}
  & None (Base Agent)  &0.7471  &0.7589  &0.7748  &0.7974  \\
  & Skill Only         &0.7800  &0.7711  &0.7881  &0.8115 \\
  & Tool Only          &0.7971  &0.7722  &0.8609  &0.8161  \\
  & Skill + Tool       &0.8500  &0.7856  &0.8742  &0.859375  \\

\midrule

\multirow{4}{*}{Doubao-Seed-2.0}
  & None (Base Agent)  &0.8214  &0.8544  &0.8675  &0.8152  \\
  & Skill Only         &0.8629  &0.8433  &0.8675  &0.8229 \\
  & Tool Only          &0.9000  &0.8700  &0.9470  &0.8078  \\
  & Skill + Tool       &0.9057  &0.8800  &0.9536  &0.86875  \\

\midrule

\multirow{4}{*}{Qwen3.5-27B}
  & None (Base Agent)  &0.8171  &0.8088  &0.88079  &0.8114  \\
  & Skill Only         &0.8192  &0.8244  &0.86755  &0.7994  \\
  & Tool Only          &0.8201  &0.8233  &0.96027  &0.8067  \\
  & Skill + Tool       &0.8372  &0.8222  &0.94702  &0.8182  \\

% \midrule

% \multirow{4}{*}{Gemini-3.1-Pro-Preview}
%   & None (Base Agent)  &0.7792  &0.8473  &0.8411  &0.6517  \\
%   & Skill Only         &  &  &  &  \\
%   & Tool Only          &  &  &  &  \\
%   & Skill + Tool       &  &0.849  &0.8543  &0.7980  \\

\bottomrule
\end{tabular}
\caption{%
  \textbf{Exp~I: Autonomous evolution from scratch.}
  Accuracy for each evolution strategy across six foundation-model blocks and four
  benchmarks, all starting from an empty capability set.
  \emph{None} is the zero-shot VLM baseline.
  \emph{Skill Only} / \emph{Tool Only} ablate one capability type.
  \emph{Skill~$+$~Tool} is the full \sysname{} system.
  Blank cells indicate runs whose results have not yet been inserted.
}
\label{tab:evolution_full}
\end{table*}

\paragraph{Distribution-shift adaptation.}
The previous comparison evaluates each benchmark independently. We also test whether evolved
capabilities remain useful in a non-stationary stream where the incoming task family changes
over time. The stream starts with 48 HRBench cases, then shifts to mixtures dominated by
MathVista, ChartQA, and finally V$^*$. This setting is a controlled replay over completed
per-case model outputs: it isolates the adaptation policy from the cost and stochasticity of
regenerating skills and tools online.

We compare four policies.
\emph{Direct VLM} uses no persistent capability pack.
\emph{Static HRBench-only} starts with the HRBench pack and never adapts.
\emph{Online adaptive} also starts with HRBench, but detects new benchmark families in a rolling
window and activates the matching capability pack after a small latency.
\emph{Oracle all packs} is an upper bound that has all task-family packs available from the
beginning.

Table~\ref{tab:distribution_shift} shows that online adaptation is consistently closer to the
oracle than to the static policy. On the main GPT-5.4 capability-sensitive stream, online
adaptation reaches 0.889 accuracy, compared with 0.654 for static HRBench-only and 0.909 for
the oracle. The stress stream makes the same point more sharply: when the stream is selected to
contain cases fixed by the relevant capability pack, direct inference nearly collapses, static
HRBench-only reaches 0.394, and online adaptation recovers to 0.952. On the natural fixed-seed
stream, where many examples do not require a newly activated capability, the gain is smaller but
still clear (0.832 vs.\ 0.736 static). Kimi and o4-mini profiles show the same qualitative
pattern, including on 200-seed natural aggregates.

\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.12}
\begin{tabular}{llcccc}
\toprule
\textbf{Model/profile} & \textbf{Protocol}
  & \textbf{Direct}
  & \textbf{Static HR}
  & \textbf{Online adapt}
  & \textbf{Oracle} \\
\midrule
GPT-5.4 skill+tool & Capability-sensitive & 0.490 & 0.654 & 0.889 & 0.909 \\
GPT-5.4 skill+tool & Stress & 0.029 & 0.394 & 0.952 & 1.000 \\
GPT-5.4 skill+tool & Natural, seed 33 & 0.692 & 0.736 & 0.832 & 0.851 \\
\midrule
Kimi-K2.6 skill-only & Capability-sensitive & 0.495 & 0.649 & 0.861 & 0.885 \\
o4-mini best-available & Capability-sensitive & 0.481 & 0.649 & 0.870 & 0.894 \\
\midrule
Kimi-K2.6 skill-only & Natural, 200-seed mean & 0.781 & 0.804 & 0.836 & 0.839 \\
o4-mini best-available & Natural, 200-seed mean & 0.745 & 0.787 & 0.807 & 0.808 \\
\bottomrule
\end{tabular}
\caption{%
  \textbf{Exp~I: Distribution-shift adaptation in a non-stationary stream.}
  The stream begins with HRBench-only capabilities and later introduces MathVista, ChartQA, and
  V$^*$ cases. \emph{Static HR} never adapts beyond the initial HRBench pack; \emph{Online
  adapt} detects new task families and activates the corresponding capability pack; \emph{Oracle}
  has all packs from the beginning.
}
\label{tab:distribution_shift}
\end{table*}

% ============================================================
\subsection{Experiment~II: Learning to Use Pre-Provided Tools}
\label{sec:exp2}
% ============================================================

\paragraph{Motivation.}
In many deployment settings, practitioners already have access to a curated tool library
(e.g.\ enterprise OCR APIs, standard CV pipelines) and wish to leverage these without
writing new tools from scratch.
A natural question is whether failure-driven skill and mastery learning can improve how a
frozen VLM uses such a provided tool set, without generating new tools or updating weights.
Experiment~II addresses this question using the GTA benchmark, which provides step-level
tool-use metrics in addition to final answer accuracy. Because the current table compares only
the base agent and \sysname{} Mode~B, we use it to measure the net effect of mastery-guided
capability use; a separate unconditional-tool baseline would be needed to isolate naive tool
overuse directly.

\paragraph{Results on GTA.}
Table~\ref{tab:gta_detailed} compares \emph{Base Agent} and \emph{Skill Evolution
(Ours)} across three foundation models on both step-level and final-answer metrics.
\emph{Base Agent} invokes the frozen VLM directly with no evolved capabilities.
\emph{Skill Evolution (Ours)} applies \sysname{} Mode~B: the mastery profiling phase
runs on the provided GTA tool set, learning per-tool applicability SOPs, and skill
evolution proceeds in parallel to refine task-level reasoning strategies.

The results expose a model-dependent interaction between skill evolution and tool-use
fidelity.
GPT-5.4 and Gemini3Flash both improve on all four metrics: GPT-5.4 gains
$+3.1$ on \textbf{AnsAcc} (72.73 $\to$ 76.03) and Gemini gains $+2.5$ (66.56 $\to$ 69.06),
accompanied by consistent improvements in \textbf{ToolAcc}, \textbf{ArgAcc}, and
\textbf{InstAcc}.
The step-level gains indicate that the evolved skills teach these models \emph{when} to
invoke tools and how to formulate arguments precisely, reducing both missed invocations
and over-invocation on unsuitable inputs.

GPT-4o, by contrast, shows a slight regression in \textbf{AnsAcc} (62.81 $\to$ 61.16)
despite comparable \textbf{ToolAcc}.
Inspection of failure cases suggests that the skills evolved from GPT-4o's training
failures are somewhat over-prescriptive: they recommend tool invocation in a broader
set of conditions than GPT-4o can reliably execute, leading the agent to invoke tools
on cases it would have solved correctly without them.
This model-capacity interaction is consistent with the Exp~I finding that evolution
gains depend on the base model's instruction-following fidelity: a model strong enough
to follow a nuanced mastery SOP benefits from skill evolution, whereas a weaker model
may be hurt if the evolved strategy demands more precise argument construction than the
model can deliver.

\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{llcccc}
\toprule
\textbf{Model} & \textbf{Method}
  & \textbf{ToolAcc}
  & \textbf{ArgAcc}
  & \textbf{InstAcc}
  & \textbf{AnsAcc} \\
\midrule

\multirow{2}{*}{GPT-4o}
  & Base Agent             & 76.4 & 69.8 & 66.2 & 62.81 \\
  & Skill Evolution (Ours) & 74.9 & 68.1 & 64.7 & 61.16 \\

\midrule

\multirow{2}{*}{GPT-5.4}
  & Base Agent             & 84.7 & 79.5 & 75.8 & 72.73 \\
  & Skill Evolution (Ours) & 87.9 & 82.8 & 79.1 & 76.03 \\

\midrule

\multirow{2}{*}{Gemini3Flash}
  & Base Agent             & 79.3 & 73.1 & 69.4 & 66.56 \\
  & Skill Evolution (Ours) & 81.8 & 75.7 & 71.9 & 69.06 \\

\bottomrule
\end{tabular}
\caption{%
  \textbf{Exp~II: Learning to use pre-provided tools on GTA.}
  We report step-level metrics (\textbf{ToolAcc}: correct tool selected;
  \textbf{ArgAcc}: correct arguments; \textbf{InstAcc}: instruction-following fidelity)
  and final task accuracy (\textbf{AnsAcc}).
  \emph{Skill Evolution (Ours)} applies \sysname{} Mode~B, which learns per-tool mastery SOPs
  from the training set without generating new tools.
}
\label{tab:gta_detailed}
\end{table*}

\paragraph{Controlled tool-policy analysis.}
To further examine what the provided-tool skills learn, we run a complementary controlled
analysis on GTA with Doubao-Seed-2.0-Pro. These runs are not a replacement for the main
multi-model comparison in Table~\ref{tab:gta_detailed}; they isolate how the learned policy
changes when either the tool abstraction or the task environment changes.
Table~\ref{tab:gta_controlled} summarizes two findings from the report.
First, when the OCR+Calculator environment is fixed, stronger composite tools compress the
policy: atomic tools improve over direct answering but use an extract-then-compute chain, while
\texttt{VisualArithmeticSolver} reaches higher accuracy with a one-call policy.
Second, when the same \texttt{OCR} tool appears in different environments, its role changes:
in OCR+Calc it feeds arithmetic, while in OCR+Search it feeds external lookup.

\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{llccccp{0.27\textwidth}}
\toprule
\textbf{Study} & \textbf{Condition} & \textbf{Cases} & \textbf{Acc.}
& \textbf{Tool use} & \textbf{Avg. calls} & \textbf{Dominant downstream behavior} \\
\midrule
Tool strength & Direct, no tools & 30 & 83.3 & 0.0 & 0.00 & \texttt{<none>} \\
Tool strength & Atomic OCR+Calc tools & 30 & 86.7 & 100.0 & 1.83
& \texttt{OCR -> Calculator} in 22/30 cases \\
Tool strength & Composite visual-arithmetic tool & 30 & 90.0 & 100.0 & 1.00
& \texttt{VisualArithmeticSolver} in 30/30 cases \\
\midrule
Same OCR tool & OCR+Calc environment & 20 & 80.0 & 100.0 & 2.20
& \texttt{Calculator} in 19/20 cases \\
Same OCR tool & OCR+Search environment & 20 & 85.0 & 100.0 & 2.35
& \texttt{GoogleSearch} in 20/20 cases \\
\bottomrule
\end{tabular}
\caption{%
  \textbf{Supplementary controlled GTA analysis for Exp~II.}
  Accuracy, tool usage rate, and average calls are reported in percent except for average calls.
  The first block fixes the task environment and changes tool strength; the second fixes the
  shared \texttt{OCR} tool and changes the downstream environment.
}
\label{tab:gta_controlled}
\end{table*}


% TODO: Insert MIRA subset results when available.

% ============================================================
\subsection{Experiment~III: Closing the Gap to VTool-R1}
\label{sec:exp3}
% ============================================================

\paragraph{Motivation.}
VTool-R1~\citep{vtoolr1_2025} is the most directly comparable prior work: it equips VLMs
with visual reasoning tools and achieves strong results on chart and table benchmarks.
Crucially, VTool-R1 achieves these results via GRPO-based reinforcement
learning~\citep{vtoolr1_2025}, requiring model weight updates.
Experiment~III asks: how much of VTool-R1's performance gain over a pure-run baseline
can be recovered by training-free skill evolution alone, without gradient updates?

This comparison is deliberately asymmetric in optimization: VTool-R1 consumes training
trajectories and performs gradient-based optimisation, whereas \sysname{} uses in-context API
calls with frozen weights.
We evaluate on the same Qwen2.5-VL backbone at three scales (3B, 7B, 32B) used in
the VTool-R1 paper to ensure the comparison is aligned.

\paragraph{Results.}
Table~\ref{tab:vtoolr1_aligned_full} shows that \sysname{} (Self-Evolved Skill) closes
a substantial fraction of the gap between \emph{Pure Run} and VTool-R1 on both the
Chart and Table reasoning splits.
Computing the gap-recovery fraction
$\frac{\text{Ours} - \text{Pure Run}}{\text{VTool-R1} - \text{Pure Run}}$
shows the strongest recovery for the 3B and 7B models. On the Chart split, the recovery is
76.1\% for 3B, 77.1\% for 7B, and 43.1\% for 32B. On the Table split, the corresponding
recoveries are 60.0\%, 94.1\%, and 40.8\%. The smaller recovery at 32B should be interpreted
with care: the pure-run baseline is already high, leaving less absolute headroom between direct
inference and VTool-R1.

This comparison is not meant to claim that skill evolution replaces RL in all settings.
VTool-R1 still achieves the highest score in every reported split, and RL can change the
model's policy in ways that in-context skills cannot. The result instead shows that a
non-trivial portion of the improvement over direct inference can be recovered without gradient
updates, especially at smaller backbone scales where the direct-inference baseline leaves more
room for task-specific procedural guidance.

\paragraph{Cost comparison.}
\sysname{} performs no weight updates. A typical evolution run uses a small training subset
($k{=}200$) for $N{=}3$ passes and invokes the frozen VLM for solving, diagnosis, generation,
and validation. This is a different resource profile from RL-based methods such as VTool-R1,
which optimize model parameters from training trajectories. The comparison therefore isolates a
practical question: how much of the gain over direct inference can be obtained when retraining is
not available?

\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{4.5pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{llcc cc}
\toprule
\textbf{Model} & \textbf{Method}
  & \textbf{Chart Split} & \textbf{Table Split}
  & \textbf{\#Train Traj.} & \textbf{RL Update} \\
\midrule

\multirow{4}{*}{Qwen2.5-VL-3B}
  & Pure Run                   &45.3995  &43.0921  & ---         & \ding{55} \\
  & VTool-R1                   &64.0  &57.9   &80*256=20480             & \ding{51} \\
  & Ours (Self-Evolved Skill)  &59.56  &51.97  &3*50=150             & \ding{55} \\
  & skill+rl  &68.28  &59.54  &20480+150=20630             & \ding{51} \\

\midrule

\multirow{4}{*}{Qwen2.5-VL-7B}
  & Pure Run                   &56.0532  &56.9078  & ---         & \ding{55} \\
  & VTool-R1                   &75.18   &68.09  &80*256=20480             & \ding{51} \\
  & Ours (Self-Evolved Skill)  &75.060  &67.43  &3*50=150             & \ding{55} \\
  & skill+rl  &76.88  &69.41  &20480+150=20630            & \ding{51} \\

\midrule

\multirow{4}{*}{Qwen2.5-VL-32B}
  & Pure Run                   &81.3559  &82.8947  & ---         & \ding{55} \\
  & VTool-R1                   &86.7*  &84.5*  &80*256=20480             & \ding{51} \\
  & Ours (Self-Evolved Skill)  &83.66  &83.55  &3*50=150             & \ding{55} \\
  & skill+rl  &  &  &20480+150=20630           & \ding{51} \\

\bottomrule
\end{tabular}
\caption{%
  \textbf{Exp~III: Training-free skill evolution vs.\ VTool-R1 (RL).}
  Accuracy (\%) on the Chart and Table reasoning splits of~\citet{vtoolr1_2025},
  using the same Qwen2.5-VL backbone at three scales.
  \textbf{\#Train Traj.} is reserved for training-trajectory counts when available.
  \ding{51}~RL Update: model weights are updated via GRPO.
  \ding{55}~RL Update: weights are frozen.
  \emph{Pure Run} and \emph{Prompted Tool Use} results are replicated
  from~\citet{vtoolr1_2025} for fair comparison.
  \sysname{} closes a substantial fraction of the gap between \emph{Pure Run}
  and VTool-R1 without any gradient updates.
}
\label{tab:vtoolr1_aligned_full}
\end{table*}




% TODO: Insert combined skill+RL results when available.

% ============================================================
\subsection{Ablation Studies}
\label{sec:ablation}
% ============================================================

\paragraph{Component ablations.}
Table~\ref{tab:ablation_main} defines the ablation grid used to isolate the contribution of
each design decision. The numerical entries are pending and are therefore not interpreted here.
The planned variants remove skill generation, tool generation, mastery profiling, or multimodal
failure diagnosis while holding the GPT-4o backbone and the $k{=}200$, $N{=}3$ protocol fixed.
Once populated, this table should answer two questions that the main experiments only address
indirectly: whether skill/tool generation contributes beyond either capability type alone, and
whether visual diagnosis and mastery profiling are necessary to avoid promoting capabilities
that solve the triggering failure but hurt other cases.

\paragraph{Sensitivity analysis.}
Appendix~\ref{app:results} contains placeholders for training-set-size and iteration-count
sensitivity. We do not draw conclusions from these analyses until the corresponding result files
are inserted.

\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{5.5pt}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{lcc}
\toprule
\textbf{Variant} & \textbf{ChartQA} & \textbf{HRBench} \\
\midrule
\sysname{} (Full)           &  &  \\
\midrule
w/o Skill Evolution         &  &  \\
w/o Tool Evolution          &  &  \\
w/o Mastery Phase           &  &  \\
w/o Visual Diagnosis        &  &  \\
\bottomrule
\end{tabular}
\caption{%
  \textbf{Ablation study.}
  Each row removes one component of \sysname{} while holding all others fixed (GPT-4o
  backbone; $k{=}200$, $N{=}3$).
  \emph{w/o Skill Evolution}: tool evolution only.
  \emph{w/o Tool Evolution}: skill evolution only.
  \emph{w/o Mastery Phase}: tools deployed unconditionally without applicability SOPs.
  \emph{w/o Visual Diagnosis}: AnalyzerDecider receives only text (question, answer,
  trace) with no image input.
  Accuracy.
}
\label{tab:ablation_main}
\end{table*}
